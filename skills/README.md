# Standalone skills

This directory contains standalone Claude Code skills.

Each skill lives in its own folder and can be installed by copying that folder into:

- `~/.claude/skills/` for personal use
- `<project>/.claude/skills/` for a single project

Unlike the entries under `plugins/`, the folders here are source skills rather than marketplace plugin packages.
